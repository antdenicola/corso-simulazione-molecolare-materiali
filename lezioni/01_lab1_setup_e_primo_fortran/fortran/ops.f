c     Exercise 2: extend the "sum" program.
c     Read two numbers a and b, then print:
c       sum, difference, product, division,
c       sqrt(a), a squared, sin(a), cos(a), natural log of a
c     Remember: statements start at column 7.
      Program Ops
      write(6,*) 'enter a'
      read(5,*) a
      write(6,*) 'enter b'
      read(5,*) b
c     TODO: compute and print the results here
      stop
      end
